module.exports = {
    usersModels: require('./users.models'),
    organizationsModels: require('./organizations.models'),
    meetingsModels: require('./meetings.models'),
    sessionsModels: require('./sessions.models'),
    tagsModels: require('./tags.models')
}
    